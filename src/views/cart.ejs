<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Đặt cơm CG</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
      integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm"
      crossorigin="anonymous"
    />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css"
    />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <%- include('header') -%>
    <div class="container">
      <div class="order-container">
        <h1 style="margin-bottom: 10px">Thông tin đơn hàng</h1>
        <h6 style="text-align: left">
          Ngày tạo đơn <%= data.dateOrder.toLocaleString() %>
        </h6>
        <hr />
        <% if(data.foods.length > 0) { %> <%
        data.foods.forEach((item) => { %>
            <% console.log(item) %>
        <div class="order-card">
          <img
            src="<%= item.food.imageUrl %>"
            alt="<%=item.food.type%> <%= item.food.name %>"
          />
          <div>
            <h4><%= item.food.name %></h4>
            <br />
            <h6><%= item.food.type %></h6>
          </div>
        </div>
        <hr />
        <% }) %> <% } else { %>
        <h6>
          Rỏ hàng hiện tại đang trống, vào <a href="/">đây</a> để mua hàng ngay!
        </h6>
        <% } %>
        <button class="btn btn-primary btn-admin">Thanh toán</button>
      </div>
    </div>
  </body>
</html>
